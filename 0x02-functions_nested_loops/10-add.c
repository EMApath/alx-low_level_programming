#include "main.h"
/**
 * add - adds two numbers and returns the result
 * @a: numbers to be added
 * @b: numbers to be added
 * Return: result
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
